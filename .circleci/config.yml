version: 2.1
orbs:
  elixir: coletiv/elixir@0.0.1
jobs:
  build-and-test:
    steps:
      - elixir/build
      - elixir/check-format
      - elixir/coveralls
      - elixir/credo
      - elixir/dialyzer
      
workflows:
  elixir-build-deploy-example:
    jobs:
      - build-and-test
